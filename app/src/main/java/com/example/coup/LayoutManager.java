package com.example.coup;

import com.example.coup.Action.Action;
import com.example.coup.Action.Coup;

public class LayoutManager {

   /* public Action next(Player currentPlayer) {

        if (currentPlayer.getCoins() >= 10) {
            // if player has more than 10 coins, Action Coup is required (activate button Coup)
        } else {

            // activate buttons for other actions
        }

        // read choosen action
        //Action choosenAction = new



        return choosenAction;
    }*/
}
